version: '3.8'

services:
  gateway:
    build: ./api-gateway
    port:
    - "8080:8080"
    depends-on:
    - user-service
    - product-service

  user-service:
    build: ./user-service
    port:
    - "3000"

  product-service:
    build: ./product-service
    port:
    - "3000"